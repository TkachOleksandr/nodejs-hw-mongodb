description: Error response
content:
  application/json:
    schema:
      $ref: '../schemas/Error.yaml'
    examples:
      notFound:
        value:
          status: 'error'
          message: 'Not found'
