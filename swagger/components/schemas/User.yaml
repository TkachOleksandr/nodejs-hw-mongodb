User:
  type: object
  properties:
    _id:
      type: string
      description: Унікальний ідентифікатор користувача
    email:
      type: string
    password:
      type: string
    avatarURL:
      type: string
      format: uri
    verified:
      type: boolean
  required:
    - email
    - password
